%br
%br
%h3
  = @slug
%br
%h5
  %span.glyphicon.glyphicon-ok
  = @first_reading.chapter.book_name
  = @first_reading.chapter.chapter_number
  - if @first_reading != @last_reading
    \-
    = @last_reading.chapter.book_name
    = @last_reading.chapter.chapter_number

  %p
  Thank you for entering your reading!
  %br
  %br
  = link_to "Go to Challenge", challenge_path(@first_reading.challenge, anchor: "statistics")
  %br
  %br
  .fb-share-button{"data-href" => "#{ENV['APP_URL']+'/challenges/'+@slug}", "layout" => "button", "data-width" => "450"}
%br


:javascript
  window.fbAsyncInit = function() {
    FB.init({
      appId      : '#{ENV['FACEBOOK_APP_ID']}',
      xfbml      : true,
      status    : true
    });
  };

  (function(d, s, id){
    var js, fjs = d.getElementsByTagName(s)[0];
    if (d.getElementById(id)) {return;}
    js = d.createElement(s); js.id = id;
    js.src = "//connect.facebook.net/en_US/sdk.js#xfbml=1&version=v2.5&appId=#{ENV['FACEBOOK_APP_ID']}";
    fjs.parentNode.insertBefore(js, fjs);
  }(document, 'script', 'facebook-jssdk'));
