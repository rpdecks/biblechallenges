- if @challenge.has_ungrouped_member?(current_user) && @challenge.owner != current_user
  = link_to "Create a group", new_member_challenge_group_path(challenge_id: @challenge.id), :class => 'btn btn-success btn-sm'
- else
  = link_to "Add a group", new_member_challenge_group_path(challenge_id: @challenge.id), :class => 'btn btn-success btn-sm'
