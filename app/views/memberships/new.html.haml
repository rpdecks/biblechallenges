%legend 
  Join the
  = @challenge.name
  Bible Reading Challenge!
%p 
  Sign up to join the #{@challenge.name} Bible Reading Challenge.  Each
  day's reading portion will be sent to your inbox, and you can click 
  a link in the email to easily update your stats online.

%h4 Challenge Details

%table.table
  %tr
    %td Begin Date:
    %td= @challenge.begindate
  %tr
    %td End Date:
    %td= @challenge.enddate
  %tr
    %td Bible Chapters:
    %td= @challenge.chapterstoread

  
= form_for @membership do |f|
  - if @membership.errors.any?
    - flash[:error] = @membership.errors.full_messages.to_sentence
  .control-group
    = f.label :email, "Email", class: "control-label"
    .controls
      = f.text_field :email, class: "input-xlarge"
      %p.help-block Enter your email to sign up for the ....challenge
  .control-group
    = f.submit "Join the Challenge"
